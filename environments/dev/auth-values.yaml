# Dev values for the auth service

replicas: 1

ingress:
  enabled: true
  path: /auth

hpa:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 70

eso:
  enabled: true

env: {}
