# Dev values for the signed-worker
# KEDA scales based on SQS signed queue depth — 0 to 3 pods

replicas: 1

ingress:
  enabled: false

hpa:
  enabled: false

keda:
  enabled: true
  minReplicaCount: 0
  maxReplicaCount: 3
  queueLength: "10"

eso:
  enabled: true

env: {}
