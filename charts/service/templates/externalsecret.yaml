# Rendered only if eso.enabled=true
# ExternalSecret — tells ESO controller to fetch secrets from AWS Secrets Manager
# and create a Kubernetes Secret that pods can use as env vars
# TODO: fill in during Phase 3/4
